# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: bedard.photography::lang.galleries.list.name
        searchable: true

    slug:
        label: bedard.photography::lang.galleries.list.slug
        searchable: true

    photo_count:
        label: bedard.photography::lang.galleries.list.photo_count

    created_at:
        label: bedard.photography::lang.galleries.list.created_at
        type: date

    updated_at:
        label: bedard.photography::lang.galleries.list.updated_at
        type: date
