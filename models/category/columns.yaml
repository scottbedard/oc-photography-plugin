# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: bedard.photography::lang.categories.list.name
        searchable: true

    slug:
        label: bedard.photography::lang.categories.list.slug
        searchable: true
